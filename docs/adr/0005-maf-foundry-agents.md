# Microsoft Agent Framework agents on Foundry models, code supervisor

Agents are wrapped with Microsoft Agent Framework using Azure AI Foundry model deployments and Foundry trace export, while the Supervisor stays plain code. We skip Foundry Agent Service persistent hosted agents to avoid provisioning risk during the hackathon; the mockable client seam keeps offline DEMO_MODE working.
